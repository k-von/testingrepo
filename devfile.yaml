schemaVersion: 2.1.0
metadata:
  name: sams-dev
projects:
  -  name: sams-cola
     git:
       remotes: 
         origin: 'git@github.com:la-county-isd/sams-cola.git'
       checkoutFrom:
         revision: production
components:
  - name: innerloop
    image:
      imageName: sams-cola:latest
      dockerfile:
        git:
          fileLocation: Dockerfile
          checkoutFrom:
            revision: 'main'
            remote: 'origin'
          remotes:
            'origin': https://github.com/brandnewbox/cola-s2i
        buildContext: .
        
 
